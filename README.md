# dsp-flow-reference-content-generator

A content generator repsotiory for "dsp-flow-reference".

Use `main.py` to generate content, then use

```bash
$ cd content/ && npx prettier . --write
```

to format the generated markdown files.
